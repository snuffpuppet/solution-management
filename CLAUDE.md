# Project instructions

Version 2.0, 9 September 2026. Owner: Adam Moyes.

This file is auto-loaded from either `ingester/` or `engagements/*` as a
parent directory, so it holds only rules that apply wherever you are
working. For the pipeline, the layout and the commit discipline, read
`ingester/CLAUDE.md` when working in the ingester, or the engagement's own
`CLAUDE.md` when working in an engagement.

## Language and versions

Australian English. No em dashes in any document. Bump the version line of
any document you change.

## Git

The owner runs every git command personally: branch, add, commit, push,
checkout, merge and all others. An agent may read git state but must not
mutate the repository via git.
