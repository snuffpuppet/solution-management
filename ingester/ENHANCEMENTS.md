# Enhancements

Version 1.1, 9 September 2026. Owner: Adam Moyes.

Work agreed for a later session. An entry is an idea with its rationale and the evidence that would justify doing it, not a commitment. When one is done, move its line to `LOG.md` with the commit. When one is dropped, delete it and say why in `LOG.md`.

Format: id, title, why, trigger to start, size.

| Id | Title | Why | Start when | Size |
|---|---|---|---|---|
| E1 | Two-model T2 comparison as a per-session option | Running T2 under two models and turning disagreements into questions raises quality on high-stakes sessions. Permitted by decision D7. | Fable access arrives, and a session is judged high stakes. | Small: a section 9 procedure already sketches it; needs a skill argument and an Audit convention. |
| E2 | Read-only self-check pass under a stronger model | A weaker model classifies, a stronger one re-reads and changes only confidence. Permitted by D7 as the one safe delegation. | The Audit table shows Opus 5 strict has a change rate the owner finds too high. | Medium: a new T2b stage, a dispatch with model parameter, Audit row. |
| E3 | Grow the appendix from real sessions | Every question whose answer overturned a class is a candidate worked example. Opus benefits most. | After the first three real sessions. | Small per row; recurring. |
| E4 | Named phases on requirements | Model 4.2 allows only this phase or next phase on REQ, while a deferred CR and a limitation's options name a later phase (I18 says "a later phase"). | Owner decides whether phases are named on REQ. | Small: model 4.2, 4.4, I2, I18. |
| E5 | End-to-end test of the register runner on transcript claims | Spec section 10 asks for it. The local target (D11) removes the Confluence dependency, so the first real ingestion is the test. | First real run of `/ingest-transcript` end to end. | Small: run and record in LOG.md. |
| E13 | Confluence target verification | Runner section 7.2 and Phase 4 describe the Confluence target but no run has exercised it since the local target became the default. | Owner wants the registers mirrored into Confluence. | Medium: a Confluence session, page read-back checks, and a decision on which target is the record. |
| E6 | Pin the knowledge base form | The runner discovers graph versus claims folder at T0. Once the real knowledge base tool is known, pin the T4 merge to its schema. | The knowledge base tool is chosen. | Medium: T4 step 4 and a fixture. |
| E7 | Comma timestamps in parser output | SRT-derived VTT uses `HH:MM:SS,mmm`. The parser normalises commas for arithmetic but prints them as found. | A transcript with comma timestamps appears. | Small: one substitution in awk and a test. |
| E8 | Speaker tag heuristics for other exporters | Zoom and Google Meet exports use other speaker conventions. | A transcript from another tool appears. | Small to medium: parser branch, fixture, test. |
| E9 | Push main to GitHub | Main is local only. | Owner says push. | Trivial. |
| E10 | Learning loop that reduces questions over time | Implements P12. A post-T4 pass reads each question and answer and writes what rule, evidence in the transcript, or human judgement would have settled it; the findings go in the session summary and feed a self-improvement stage that proposes runner document edits for human approval (P1, P2, P10), measured by the Audit change rate (P6). Automates E3. | At least three real sessions have Audit rows. | Medium: a summary section, a findings file, a proposal procedure, a check. |
| E11 | Runner version on each claim record | A claim outlives its session; `runner_model` says who classified it but not which guide rules were in force. A `runner_version` field would let old claims be listed for re-check after a guide edit, and let register Phase 2 treat them with more caution. Widens the P7 interface by one field; the Audit table join covers it manually meanwhile. | The first transcript runner edit motivated by Audit data lands and someone needs to find claims produced under the old rules. | Small: claim record table and example in runner section 6, report claims-by-class column, runner check assertion. |
| E12 | Expertise on stakeholders | `transcripts/stakeholders.md` carries a blank Expertise column. Filling it could let T2 weight a claim by whether the speaker is expert in its topic, and let the register runner propose Owner or Consulted. Kept blank until real sessions show which questions it would answer. | Three sessions have shown a repeated question that a person's expertise would have settled. | Small: registry rows, one guide sentence, a check. |
| E14 | Split claims quote their own clause | Section 6 says `quote` is "verbatim passage text", so all parts of a split passage carry the same full quote. On T001 three claims from passage 316 share one 500 character quote that is mostly about a different claim. A reader of one claim cannot see which words support it, weakening P2 traceability. The `passage` field's letter suffix is the only pointer. Source: abb-nokia F1. | A register runner session where a reviewer cannot tell which clause supports a claim. | Small to medium: section 6 quote rule, T2 recording the clause boundary, T3 carrying it. |
| E15 | Trivial context claims inflate the claims file | T3 step 1 writes one claim per classified passage, so backchannels like "Yeah." and "Correct." become full claim objects. On T001 that is 384 of 442 claims, roughly 87 percent, each with fifteen fields the register runner must skip on every read, growing with every session. Candidates: a `trivial` flag on context claims, or a T3 rule that a short context claim with no relation is not written. Source: abb-nokia F2. | The claims folder reaches a size where register runner Phase 1 inventory becomes slow or costly. | Small: one T3 rule plus a claim record field and a check. |
| E16 | Parser leaves HTML entities undecoded | `tools/vtt-to-passages.sh` passes `&amp;` through to passage text, so a speaker saying "S and C tag" appears as `S&amp; C tag` and that string lands in a claim quote verbatim. One occurrence on T001, at passage 181. Other entities (`&lt;`, `&gt;`, `&quot;`, `&#39;`) would behave the same. Source: abb-nokia F3. | Any transcript where an entity appears inside a quote that matters. | Small: one substitution in awk and a fixture assertion. |
| E17 | check-roles.sh asserts only one role's class list | The checker has a `need` line for the `sme` May yield list but none for the other four rows, so the class lists of `consultant`, `vendor`, `architect` and `unknown` are unasserted. On 2026-09-09 a reformat deleted `system` from four cells and only the `sme` deletion was caught; the other three were invisible. The derived `classes` loop checks that every class named is known to the transcript runner, but not that any particular class is still named. | Now, or the next time a role row is edited. | Small: four `need` lines, or one loop over an expected class list per role. |
